package log

import (
    "testing"
)

func fakeTest(t *testing.T) {
    //empty test for now, I need to refactor the logger to dependency inject a logger interface so that I can inject a spy object in it to verify calls were made
}
